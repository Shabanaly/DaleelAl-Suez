دليل السويس

موقع دليل مدينة (السويس)
يعرض أماكن وخدمات محلية
الربح من الإعلانات فقط
بدون بيع أو تواصل مع أصحاب الأماكن حاليًا


HTML
CSS
JavaScript (Vanilla فقط)


/index.html                     ← الصفحة الرئيسية

/categories/
   restaurants.html             ← مطاعم
   cafes.html                   ← كافيهات
   doctors.html                 ← أطباء
   pharmacies.html              ← صيدليات
   shops.html                   ← محلات
   services.html                ← خدمات

/subcategories/
   restaurants-grill.html       ← مطاعم مشويات
   restaurants-seafood.html     ← مطاعم سمك
   cafes-sea-view.html          ← كافيهات على البحر
   doctors-dentist.html         ← أطباء أسنان
   services-car.html            ← خدمات سيارات

/places/
   restaurant-abu-ali.html
   restaurant-sea-food.html
   cafe-blue.html
   doctor-ahmed.html
   pharmacy-elshifa.html

/pages/
   about.html                   ← عن الموقع
   contact.html                 ← تواصل معنا
   privacy.html                 ← سياسة الخصوصية
   terms.html                   ← الشروط
   advertise.html               ← أعلن معنا (لاحقًا)

/css/
   reset.css
   style.css
   layout.css
   responsive.css

/js/
   data/
      restaurants.js
      cafes.js
      doctors.js
      pharmacies.js
      services.js

   templates.js                 ← Templates HTML
   render.js                    ← عرض البيانات
   filter.js                    ← فلترة
   search.js                    ← بحث
   main.js                      ← تشغيل عام

/assets/
   /images/
      logo.png
      default-place.jpg
   /icons/
   /fonts/

/seo/
   sitemap.xml
   robots.txt
← العرض والبحث


- اسم الموقع
- وصف رئيسي (H1)
- وصف ثانوي (H2)
- أقسام رئيسية
- أحدث الأماكن
- رسالة "الدليل تحت التطوير"


- عنوان القسم (H1)
- وصف القسم (SEO)
- روابط للأقسام الفرعية
- قائمة أماكن


- عنوان دقيق (مطاعم مشويات في السويس)
- وصف مخصص
- قائمة أماكن متخصصة


- اسم المكان (H1)
- التصنيف
- العنوان
- رقم الهاتف
- مواعيد العمل
- وصف نصي
- صورة
- خريطة Google Maps iframe


about.html       ← من أنت ولماذا الموقع
contact.html     ← وسيلة تواصل
privacy.html     ← سياسة الخصوصية (مهمة للإعلانات)
terms.html
advertise.html   ← جاهزة للمستقبل


- كل قسم له ملف JS خاص
- البيانات Arrays
- كل مكان Object


- عرض الأماكن
- البحث
- الفلترة
- ترتيب بسيط


- تسجيل دخول
جوجل أوث    
وفيس بوك أوث


- إعلانات Google AdSense
- أماكن إعلانات محجوزة في التصميم
- التفعيل لاحقًا

- لا اقتراح Features
- لا تغيير Stack
- لا تحسينات من تلقاء نفسه
- تنفيذ حرفي فقط


🧱 STRUCTURE كامل للموقع + لوحة التحكم (Dashboard)

هدف المشروع:
موقع دليل مدينة مع لوحة تحكم لإدارة المحتوى.
الموقع Frontend يعرض البيانات فقط.
لوحة التحكم هي المسؤولة عن إضافة وتعديل وحذف البيانات.
الربط بين الموقع والداشبورد يتم عبر مصدر بيانات واحد (Database أو Data Layer).

==================================================

📁 هيكل المشروع (Project Structure)

/index.html

/categories/
   restaurants.html
   cafes.html
   doctors.html

/subcategories/
   restaurants-grill.html
   restaurants-seafood.html

/places/
   place-details.html        ← Template واحد يعرض المكان حسب ID

/dashboard/
   login.html                ← صفحة تسجيل الدخول (واجهة فقط)
   index.html                ← Dashboard Home
   places.html               ← إدارة الأماكن
   categories.html           ← إدارة الأقسام
   subcategories.html        ← إدارة الأقسام الفرعية
   ads.html                  ← إدارة الإعلانات
   settings.html             ← إعدادات الموقع

/css/
   style.css                 ← ستايل الموقع العام
   dashboard.css             ← ستايل لوحة التحكم

/js/
   data/                     ← (مصدر البيانات – يتم استبداله لاحقًا بقاعدة بيانات)
      places.js
      categories.js
      subcategories.js

   dashboard/
      auth.js                ← منطق تسجيل الدخول (مؤقت)
      dashboard.js           ← تشغيل الصفحة الرئيسية للوحة التحكم
      places-manager.js      ← إضافة / تعديل / حذف الأماكن
      categories-manager.js  ← إدارة الأقسام
      ads-manager.js         ← إدارة الإعلانات

   render.js                 ← عرض البيانات في الموقع
   main.js                   ← تشغيل الموقع العام

/assets/
   images/
   icons/

/seo/
   sitemap.xml
   robots.txt

==================================================

🖥️ Dashboard Pages & Functionalities

1️⃣ Dashboard Home – /dashboard/index.html

تعرض إحصائيات عامة:
- عدد الأماكن
- عدد الأقسام
- عدد الصفحات
- آخر مكان تمت إضافته

==================================================

2️⃣ إدارة الأماكن – /dashboard/places.html

تحتوي على:
- جدول يعرض كل الأماكن
- زر إضافة مكان
- زر تعديل مكان
- زر حذف مكان

نموذج إضافة / تعديل مكان يحتوي على:
- اسم المكان
- القسم
- القسم الفرعي
- العنوان
- الهاتف
- مواعيد العمل
- وصف المكان
- رابط الخريطة (Google Maps)

==================================================

3️⃣ إدارة الأقسام – /dashboard/categories.html

الوظائف:
- إضافة قسم جديد
- تعديل اسم قسم
- حذف قسم

==================================================

4️⃣ إدارة الأقسام الفرعية – /dashboard/subcategories.html

الوظائف:
- إضافة قسم فرعي
- ربط القسم الفرعي بقسم رئيسي
- تعديل قسم فرعي
- حذف قسم فرعي

==================================================

5️⃣ إدارة الإعلانات – /dashboard/ads.html

الوظائف:
- تحديد أماكن الإعلانات داخل الموقع
- إضافة كود الإعلان
- تشغيل / إيقاف الإعلان

ملاحظات:
- Google AdSense سيتم استخدامه لاحقًا
- حاليًا يتم استخدام Placeholders فقط

==================================================

6️⃣ الإعدادات – /dashboard/settings.html

التحكم في:
- اسم الموقع
- وصف الموقع
- رسالة "الموقع تحت التطوير"
- تشغيل / إيقاف الإعلانات

==================================================

🔐 تسجيل الدخول (Login System)

- صفحة login.html
- إدخال username + password
- تحقق Front-end فقط (مؤقت)
- حماية وهمية للمرحلة الأولى
- قابل للاستبدال لاحقًا بنظام Auth حقيقي

==================================================

🧩 شكل Object المكان (ثابت لا يتغير في كل المشروع)

{
  id,
  name,
  category,
  subCategory,
  address,
  phone,
  hours,
  description,
  map,
  image
}

==================================================

🔄 العلاقة بين لوحة التحكم والموقع

Dashboard  ➜  تعدل البيانات
Data Source ➜  يتم تحديثه
Frontend   ➜  يعرض البيانات تلقائيًا

- الموقع لا يضيف ولا يعدل بيانات
- لوحة التحكم هي المصدر الوحيد للإدارة
- نفس هيكل البيانات يُستخدم في كل المشروع

==================================================
🔗 ربط البيانات باستخدام Supabase (Database Layer)
==================================================

🎯 الهدف من هذا الجزء:
- استخدام Supabase كمصدر البيانات الوحيد (Single Source of Truth)
- ربط لوحة التحكم (Dashboard) بقاعدة البيانات مباشرة
- الموقع العام (Frontend) يقرأ فقط من قاعدة البيانات
- منع استخدام LocalStorage أو ملفات بيانات محلية نهائيًا

==================================================

🗄️ مزود قاعدة البيانات (Database Provider)

- Supabase (PostgreSQL)
- Backend as a Service
- REST + JS Client جاهز
- Free Plan مناسب للمشروع

==================================================

🧱 الجداول الأساسية في قاعدة البيانات (Schema)

1️⃣ جدول الأقسام (categories)
----------------------------------
id              (integer, primary key)
name            (text)
slug            (text)
created_at      (timestamp)

2️⃣ جدول الأقسام الفرعية (subcategories)
----------------------------------
id              (integer, primary key)
category_id     (integer, foreign key -> categories.id)
name            (text)
slug            (text)
created_at      (timestamp)

3️⃣ جدول الأماكن (places)
----------------------------------
id              (integer, primary key)
name            (text)
slug            (text)
category_id     (integer, foreign key)
subcategory_id  (integer, foreign key)
address         (text)
phone           (text)
working_hours   (text)
description     (text)
map_url         (text)
image           (text)
is_active       (boolean)
created_at      (timestamp)

4️⃣ جدول الإعلانات (ads)
----------------------------------
id              (integer, primary key)
position        (text)
code            (text)
is_active       (boolean)

5️⃣ جدول الإعدادات (settings)
----------------------------------
key             (text, primary key)
value           (text)

==================================================

🧩 شكل Object المكان (ثابت لا يتغير)

{
  id,
  name,
  category,
  subCategory,
  address,
  phone,
  hours,
  description,
  map,
  image
}

- نفس الشكل يُستخدم في:
  • Dashboard
  • Frontend
  • Database

==================================================

🔌 إعداد الاتصال بـ Supabase

1️⃣ إنشاء مشروع جديد على Supabase
- إنشاء Project
- الحصول على:
  • SUPABASE_URL
  • SUPABASE_ANON_KEY

2️⃣ إنشاء ملف اتصال عام
----------------------------------
/js/supabase-client.js
----------------------------------

import { createClient } from "https://cdn.jsdelivr.net/npm/@supabase/supabase-js/+esm";

const SUPABASE_URL = "YOUR_SUPABASE_URL";
const SUPABASE_ANON_KEY = "YOUR_SUPABASE_ANON_KEY";

export const supabase = createClient(
  SUPABASE_URL,
  SUPABASE_ANON_KEY
);

==================================================

🖥️ ربط لوحة التحكم (Dashboard) بـ Supabase

📌 لوحة التحكم = CRUD كامل

مثال: جلب كل الأماكن (Dashboard)
----------------------------------
const { data, error } = await supabase
  .from("places")
  .select("*")
  .order("created_at", { ascending: false });

مثال: إضافة مكان جديد
----------------------------------
await supabase.from("places").insert([
  {
    name: "مطعم أبو علي",
    category_id: 1,
    subcategory_id: 2,
    address: "السويس",
    phone: "01000000000",
    working_hours: "12:00 - 12:00",
    description: "مطعم مشويات",
    map_url: "...",
    image: "...",
    is_active: true
  }
]);
